name: Release

on:
  push:
    branches:
      - release

jobs:
  release:
    name: Tag, release, and publish the version in pyproject.toml
    runs-on: ubuntu-latest
    concurrency:
      group: release-${{ github.ref }}
      cancel-in-progress: false
    environment:
      name: pypi
      url: https://pypi.org/p/soxai_data
    permissions:
      contents: write
      id-token: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Read the version to release
        id: version
        run: |
          VERSION=$(grep -E '^[[:space:]]*version[[:space:]]*=' pyproject.toml | sed -E 's/.*version[[:space:]]*=[[:space:]]*"([^"]*)".*/\1/')
          if [ -z "$VERSION" ]; then
            echo "Error: Failed to parse version from pyproject.toml" >&2
            exit 1
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Releasing v$VERSION as declared in pyproject.toml"

      - name: Fail if the version was already released
        run: |
          TAG="v${{ steps.version.outputs.version }}"
          if git rev-parse -q --verify "refs/tags/$TAG" >/dev/null; then
            echo "Error: $TAG already exists. Bump the version in pyproject.toml and" >&2
            echo "add the matching section to CHANGELOG.md before releasing again." >&2
            exit 1
          fi

      - name: Fail if CHANGELOG.md has no section for the version
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          if ! awk -v needle="## [$VERSION]" '
            index($0, needle) == 1 { found = 1 }
            END { exit !found }
          ' CHANGELOG.md; then
            echo "Error: CHANGELOG.md has no section for $VERSION." >&2
            echo "Add a '## [$VERSION] - YYYY-MM-DD' section before releasing." >&2
            exit 1
          fi

      # everything that can still fail runs before the tag and the release are created,
      # so that a failed run leaves nothing behind and can simply be run again
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Build distribution 📦
        run: |
          python3 -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install build
          python3 -m build

      - name: Publish distribution 📦 to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          # PyPI rejects a version it already holds, which would otherwise turn a rerun
          # after a failure of one of the steps below into a dead end
          skip-existing: true

      - name: Create and push tag
        run: |
          git tag "v${{ steps.version.outputs.version }}"
          git push origin "v${{ steps.version.outputs.version }}"

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "v${{ steps.version.outputs.version }}" \
            --title "v${{ steps.version.outputs.version }}" \
            --generate-notes
